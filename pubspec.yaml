name: pcc
description: "PCC - Monitoreo Cotorra Margariteña"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  fl_chart: ^0.70.0
  provider: ^6.1.2
  google_fonts: ^6.2.1
  shared_preferences: ^2.3.3
  file_picker: ^8.1.7
  intl: ^0.19.0
  shimmer: ^3.0.0
  supabase_flutter: ^2.12.4
  excel: ^4.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
